clc;
clear;
Ac=1;
fs=3e9;
fc=2e8;
fp=2e7;
dt=1/fs;
N=15000;
b=randi(2,1,15)-1
ur=zeros(1,N);
% ur=Ac*sin(2*pi*fc*t/fs).*square(2*pi*fp*t,5);
for t=1:15
    N=
    if b(t)==1
        ur(t:t+1000)=Ac*sin(2*pi*fc*t/fs);
    else
        ur(t:t+1000)=0;
    end
end
plot(ur);

